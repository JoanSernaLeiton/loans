# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build authentication-back`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t authentication-back`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system authentication-back && \
          adduser --system -G authentication-back authentication-back

COPY dist/apps/authentication-back authentication-back
RUN chown -R authentication-back:authentication-back .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix authentication-back --omit=dev -f install

